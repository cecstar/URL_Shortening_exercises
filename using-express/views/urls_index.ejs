<!DOCTYPE html>
<html lang="en">
<body>
  <header>
        <% include ./partials/_header %>
  </header>
  <ul> <% for(var key in urls) { %>
      <li> <%= link_to(key, urls[key]) %> </li>
      <% }; %>
  </ul>
    <p>more fun stuff, party on wayne!</p>
</body>
</html>
<!--   <ul> <% for(var key in urls) { %>
      <li> <%= urls[key] : .key %> </li>
      <% }; %>
  </ul> -->
